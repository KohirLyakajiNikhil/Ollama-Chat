# Contributing

Thanks for considering contributing!

- Fork the repository and create a feature branch.
- Run `pip install -r requirements-dev.txt` and `pre-commit install`.
- Run `pre-commit run --all-files` and ensure all checks pass before opening a PR.
- Include tests for new or fixed functionality and make sure `pytest` passes locally.

PR reviewers will check style, tests, and documentation.
